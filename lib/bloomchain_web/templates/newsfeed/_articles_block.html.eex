<div class="row">
  <%= for date <- @articles |> Map.keys() |> Enum.sort(&(&1 >= &2)) do %>
    <div class="col-lg-12">
      <%= if (@previous_date != to_string(date)) do %>
        <%= header_tag(date) %>
      <% end %>

      <%= for item <- @articles[date] do %>
        <%= item_tag(item) %>
      <% end %>
    </div>
  <% end %>
</div>
